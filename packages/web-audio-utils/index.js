'use strict';

const mergeParams = require('./mergeParams');
const CompositeAudioNode = require('./CompositeAudioNode');
const getLiveAudio = require('./getLiveAudio');

module.exports = {
  mergeParams,
  CompositeAudioNode,
  getLiveAudio
}
